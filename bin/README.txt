The simulation results will be saved in this folder.
Please run the codes in the main folder.

Packages needed to be installed:
pip install numpy
pip install matplotlib
pip install tqdm
pip install sympy
pip install imageio[ffmpeg]